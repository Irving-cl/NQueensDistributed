syntax = "proto2";

package nqueens;

message MasterCall {
    required string password = 1;
}

message SlaveAskForWork {
    required string password = 1;
}

message MasterAssignWork {
    repeated int32 constraint = 1;
}
